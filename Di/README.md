# DI 容器核心功能
1. 组件构造
2. 依赖的选择
3. 生命周期

```java
// Happy Path
// TODO 当注解定义在构造方法，对象存在时，成功注入
// TODO 当注解定义在字段，对象存在时，成功注入
// TODO 当注解定义在方法，对象存在时，成功注入
// TODO 当注解定义在字段或者方法上，且存在父类，先调用父类，后调用子类注入
// TODO 当被注入对象定义了 @Named，存在字符串匹配的对象时，注入成功
// TODO 当被注入对象定义了强类型自定义注解，存在该注解的绑定，注入成功
// TODO 当被注入对象定义了 @Singleton 注解，注入的都是同一个对象

// Sad Path
// TODO 当注解定义在构造方法，对象不存在时，注入失败，抛出异常
// TODO 当注解定义在字段，对象不存在时，注入失败，抛出异常
// TODO 当注解定义在方法，对象不存在时，注入失败，抛出异常
// TODO 当注解定义在字段或者方法上，当父类对应字段或者方法注入失败，抛出异常
// TODO 当被注入对象定义了 @Named，不存在字符串匹配的对象时，抛出异常
// TODO 当被注入对象定义了强类型自定义注解，不存在该注解的绑定，抛出异常

// Default Path
// TODO 当被注入对象未定义 强类型，默认使用 @Named 查找注入对象
// TODO 当注入对象未定义 @Scope 注解，默认提供 @Scope 朱恩杰，实现为多例
```